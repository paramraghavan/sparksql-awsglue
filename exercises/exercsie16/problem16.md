```markdown
For EMR cluster, create a custom scaling cluster
define in config file
- config file defines
* number of servers to scale to
* task node mix - spot isntances or on demand instances
* assigning the instance count and type (on demand or spot) based on saprk seesion asppName
```