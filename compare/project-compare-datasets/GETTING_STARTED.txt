🚀 GETTING STARTED - Parquet Comparison on EMR
═══════════════════════════════════════════════════════════════

Your solution is ready! Here's how to get started in 3 steps:

┌─────────────────────────────────────────────────────────────┐
│ STEP 1: TEST LOCALLY (OPTIONAL BUT RECOMMENDED)            │
└─────────────────────────────────────────────────────────────┘

This helps verify everything works before running on EMR:

1. Install PySpark locally:
   pip install -r requirements.txt

2. Run the test:
   python test_local.py --full

3. Check the output:
   open /tmp/test_comparison_report.html

✅ If the report looks good, you're ready for EMR!

┌─────────────────────────────────────────────────────────────┐
│ STEP 2: DEPLOY TO EMR                                      │
└─────────────────────────────────────────────────────────────┘

1. Upload the script to S3:
   aws s3 cp parquet_comparison.py s3://your-bucket/scripts/

2. Edit submit_comparison.sh with your details:
   - CLUSTER_ID: Your EMR cluster ID
   - PATH1: First parquet file location
   - PATH2: Second parquet file location
   - KEY_COLUMNS: Your key columns
   - SKIP_COLUMNS: Columns to skip (optional)
   - OUTPUT_PATH: Where to save the report

3. Submit the job:
   chmod +x submit_comparison.sh
   ./submit_comparison.sh

┌─────────────────────────────────────────────────────────────┐
│ STEP 3: MONITOR & RETRIEVE RESULTS                         │
└─────────────────────────────────────────────────────────────┘

1. Monitor via EMR Console:
   - Go to EMR Console → Your Cluster → Steps tab
   - Watch the step status

2. Or monitor via CLI:
   aws emr describe-step --cluster-id j-XXX --step-id s-XXX

3. Once complete, download the report:
   aws s3 cp s3://your-bucket/reports/report.html ./
   open comparison_report.html

┌─────────────────────────────────────────────────────────────┐
│ CONFIGURATION FOR YOUR 50GB+ FILES                         │
└─────────────────────────────────────────────────────────────┘

For your use case (50GB+, 200+ columns), recommended config:

EMR Cluster:
  - Instance Type: r5.2xlarge (64GB RAM, 8 cores)
  - Number of Nodes: 5-10
  - EBS: 100GB per node

Spark Settings (already in submit_comparison.sh):
  - Executors: 20
  - Executor Memory: 8g
  - Executor Cores: 4
  - Shuffle Partitions: 400

Expected Performance:
  - Runtime: 20-40 minutes
  - Cost: ~$5-15 per run (using Spot instances)

┌─────────────────────────────────────────────────────────────┐
│ WHAT THE COMPARISON WILL SHOW YOU                          │
└─────────────────────────────────────────────────────────────┘

The HTML report includes:

📊 Summary Dashboard
   - Total record counts for both datasets
   - Record count differences
   - Columns with value differences

🔍 Schema Analysis
   - Columns present in both datasets
   - Columns unique to each dataset

🔑 Key-Based Matching
   - Records only in Dataset 1
   - Records only in Dataset 2
   - Matching records

📋 Column Comparison
   - For each column: # of differences and %
   - Searchable table for easy navigation

🔬 Sample Differences
   - Actual value differences shown
   - Top mismatching columns highlighted
   - Up to 20 samples per column

┌─────────────────────────────────────────────────────────────┐
│ FILES IN THIS PACKAGE                                      │
└─────────────────────────────────────────────────────────────┘

parquet_comparison.py  - Main PySpark script (upload to S3)
submit_comparison.sh   - EMR job submission script
test_local.py         - Local testing script
README.md             - Complete documentation
QUICK_REFERENCE.txt   - Cheat sheet for common operations
requirements.txt      - Python dependencies

┌─────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING GUIDE                                      │
└─────────────────────────────────────────────────────────────┘

Problem: Job runs out of memory
Solution: 
  - Increase executor memory: --executor-memory 12g
  - Increase partitions: --partitions 800
  - Reduce executor cores: --executor-cores 3

Problem: Job is too slow
Solution:
  - Add more executors: --num-executors 40
  - Increase partitions: --partitions 1000
  - Check Spark UI for data skew

Problem: Job fails immediately
Solution:
  - Check S3 paths are correct
  - Verify key columns exist in both datasets
  - Check IAM permissions for S3 access
  - Review EMR cluster logs

Problem: Some columns showing 100% differences
Solution:
  - Check data types (e.g., timestamp formatting)
  - Verify column names match exactly
  - Consider adding to --skip if expected

┌─────────────────────────────────────────────────────────────┐
│ QUICK EXAMPLE                                              │
└─────────────────────────────────────────────────────────────┘

Let's say you want to compare:
  - Production data: s3://prod-bucket/sales.parquet
  - Staging data: s3://stage-bucket/sales.parquet
  - Key: transaction_id
  - Skip: loaded_timestamp, etl_run_id

Command:
spark-submit \
  --master yarn \
  --deploy-mode cluster \
  --num-executors 20 \
  --executor-memory 8g \
  --executor-cores 4 \
  s3://my-scripts/parquet_comparison.py \
  --path1 s3://prod-bucket/sales.parquet \
  --path2 s3://stage-bucket/sales.parquet \
  --keys "transaction_id" \
  --skip "loaded_timestamp,etl_run_id" \
  --name1 "Production" \
  --name2 "Staging" \
  --output s3://my-reports/sales-comparison.html \
  --partitions 400

┌─────────────────────────────────────────────────────────────┐
│ PERFORMANCE OPTIMIZATION TIPS                              │
└─────────────────────────────────────────────────────────────┘

1. Start Small
   - Test with a sample first (one partition)
   - Use: --path1 "s3://bucket/data/date=2024-11-13/*"

2. Right-Size Your Cluster
   - Data size × 4 = Total RAM needed
   - 50GB data → 200GB RAM → 4x r5.2xlarge nodes

3. Tune Partitions
   - Rule of thumb: Data size (GB) × 8 = partitions
   - 50GB → 400 partitions
   - 100GB → 800 partitions

4. Use Spot Instances
   - Save 70-90% on costs
   - Use On-Demand for master node only

5. Monitor and Adjust
   - Watch Spark UI for stragglers
   - Check memory usage in CloudWatch
   - Adjust configs between runs

┌─────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                 │
└─────────────────────────────────────────────────────────────┘

1. ☐ Review README.md for complete documentation
2. ☐ Test locally with test_local.py (optional)
3. ☐ Upload parquet_comparison.py to S3
4. ☐ Edit submit_comparison.sh with your parameters
5. ☐ Submit job to EMR
6. ☐ Monitor execution
7. ☐ Download and review HTML report
8. ☐ Check QUICK_REFERENCE.txt for common operations

┌─────────────────────────────────────────────────────────────┐
│ NEED HELP?                                                 │
└─────────────────────────────────────────────────────────────┘

Common issues and solutions are in:
  - README.md → Troubleshooting section
  - QUICK_REFERENCE.txt → Common Issues & Fixes

Check these logs if something goes wrong:
  - EMR Console → Steps → View logs
  - CloudWatch → Log Groups → /aws/emr/
  - Spark UI → Failed stages/tasks

═══════════════════════════════════════════════════════════════

🎯 YOUR MISSION: Compare those 50GB parquet files!

The solution handles:
✅ Large file sizes (50GB+)
✅ Many columns (200+)  
✅ Distributed processing on EMR
✅ Comprehensive comparison
✅ Beautiful HTML reports
✅ Performance optimizations built-in

Good luck! 🚀
