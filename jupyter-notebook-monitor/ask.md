we have modeller who sue jouyer notebooks deployed on EMR and sage maker sutdio. Is there a pluin code that can ask the
modellers to add to their starting cell, this way its gets a monitoring background gets intialized which monitors code
reat of the cells.
The modellers do not know much about EMR they thinks its a infinite resource and many time they ru jobw which overwhem
the EMR clsuter or the job executes very slow, - becasue they did not cache as the dataframe is reused or with matplot
lib fir the dataframe neeed to be cached , or dataframe need to be repartitoned, we can alert user as they run their
step, hey can skip or apply the chaneg and rerun the cells